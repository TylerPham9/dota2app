import 'package:flutter/material.dart';
import 'package:dota2app/style.dart';
import 'package:dota2app/models/Talents.dart';

class HeroTalents extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("NEW TALENTS PAGE"),
        ),
      ),
    );
  }
}